/**
 * Re-export of useProfileContext for backward compatibility
 * All profile logic is now managed by ProfileContext
 */
export { useProfileContext as useProfile } from "@/context/profile-context";


